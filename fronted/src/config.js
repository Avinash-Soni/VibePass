// Ensure there are no extra spaces or characters before 'export'
export const API_BASE_URL = window.location.hostname === "localhost" 
  ? "http://localhost:8080" 
  : "https://vibepass-944b.onrender.com";